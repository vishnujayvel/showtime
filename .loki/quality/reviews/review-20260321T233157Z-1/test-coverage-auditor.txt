VERDICT: FAIL
FINDINGS:
- [Medium] Second test (`confirming reset returns to Dark Studio`) depends on state left by the first test — it assumes Director Mode is still visible and the reset button is clickable without re-setting up localStorage state. If tests run independently or the first test fails, the second will fail too. (e2e/showtime.test.ts:1041-1043)
- [Medium] State verification uses a silent `if (state)` guard that swallows a null result instead of asserting — if localStorage is unexpectedly empty after reset, the test passes with no assertions about the reset outcome. Should use `expect(state).not.toBeNull()` before the property checks. (e2e/showtime.test.ts:1061-1065)
- [Medium] No E2E test for the tray menu "Reset Show" IPC path — the tray label is asserted in `#3 tray menu labels`, but there is no test that clicking the tray Reset Show item actually sends `IPC.RESET_SHOW` and the renderer handles it (the `onResetShow` listener in App.tsx). Only the in-app DirectorMode button path is tested. (src/main/index.ts:1216-1220, src/renderer/App.tsx:40-41)
- [Low] Both tests use `waitForTimeout` (500ms, 300ms, 1000ms) instead of waiting for specific UI state changes, which is a flaky test pattern — timing-sensitive on slow CI machines. Prefer `waitForSelector` or Playwright `expect` auto-retry assertions. (e2e/showtime.test.ts:1025, 1035, 1049)
- [Low] No unit test for `resetShow` when called during `writers_room` or `live` phases with an active timer — the store test only covers reset from `live` (post-startShow) and `strike` phases, but `resetShow` is available from DirectorMode which can appear during any live sub-phase. The `celebrationTimeout` cleanup is tested, but timer cleanup during an active act countdown is not. (src/__tests__/showStore.test.ts:619-633)
