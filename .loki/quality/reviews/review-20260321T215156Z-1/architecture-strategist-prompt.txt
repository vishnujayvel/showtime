You are architecture-strategist. Your SOLE focus is: SOLID, coupling, cohesion, patterns, abstraction, dependency direction.

Review ONLY for: SOLID violations, excessive coupling, wrong patterns, missing abstractions, dependency direction issues, god classes/functions.

Files changed:
.loki/CONTINUITY.md
.loki/STATUS.txt
.loki/app-runner/detection.json
.loki/app-runner/state.json
.loki/autonomy-state.json
.loki/context/last_offset.txt
.loki/context/tracking.json
.loki/council/state.json
.loki/dashboard-state.json
.loki/dashboard/dashboard.pid
.loki/events.jsonl
.loki/logs/agent-audit.jsonl
.loki/logs/audit-20260321.jsonl
.loki/metrics/efficiency/iteration-1.json
.loki/metrics/efficiency/iteration-2.json
.loki/pids/2623.json
.loki/prd-observations.md
.loki/quality/gate-failure-count.json
.loki/quality/static-analysis.json
.loki/quality/test-results.json
.loki/queue/completed.json
.loki/state/checkpoints/index.jsonl
.loki/state/resources.json
src/__tests__/audioAndPolish.test.tsx
src/renderer/components/BeatCheckModal.tsx
src/renderer/components/IntermissionView.tsx
src/renderer/components/MuteToggle.tsx
src/renderer/hooks/useAudio.ts
src/renderer/hooks/useTimer.ts
src/renderer/index.css
src/renderer/views/ExpandedView.tsx
src/renderer/views/GoingLiveTransition.tsx
src/renderer/views/PillView.tsx
src/renderer/views/StrikeView.tsx
src/renderer/views/WritersRoomView.tsx

Diff:
diff --git a/.loki/CONTINUITY.md b/.loki/CONTINUITY.md
index 29b2503..a7b1345 100644
--- a/.loki/CONTINUITY.md
+++ b/.loki/CONTINUITY.md
@@ -1,19 +1,20 @@
 # Session Continuity
 
-Updated: 2026-03-21T21:27:40Z
+Updated: 2026-03-21T21:51:47Z
 
 ## Current State
 
-- Iteration: 2
+- Iteration: 1
 - Phase: DEVELOPMENT
-- RARV Step: REFLECT
+- RARV Step: ACT
 - Provider: claude
 - Elapsed: 0m
 
 ## Last Completed Task
 
-- Last commit: Fix quality gate failures: RundownBar hooks violation + better-sqlite3 test loading
-- Files changed: package.json, src/renderer/components/RundownBar.tsx
+- Last commit: feat: audio broadcast package + UX polish (5 features)
+- Files changed: src/__tests__/audioAndPolish.test.tsx, src/renderer/components/BeatCheckModal.tsx, src/renderer/components/IntermissionView.tsx, src/renderer/components/MuteToggle.tsx, src/renderer/hooks/useAudio.ts
+  (+7 more)
 
 ## Active Blockers
 
diff --git a/.loki/STATUS.txt b/.loki/STATUS.txt
index d358294..9eac3ec 100644
--- a/.loki/STATUS.txt
+++ b/.loki/STATUS.txt
@@ -2,14 +2,14 @@
 ║                    LOKI MODE STATUS                            ║
 ╚════════════════════════════════════════════════════════════════╝
 
-Updated: Sat Mar 21 14:29:44 PDT 2026
+Updated: Sat Mar 21 14:51:51 PDT 2026
 
 Phase: DEVELOPMENT
 
 Tasks:
   ├─ Pending:     0
   ├─ In Progress: 0
-  ├─ Completed:   6
+  ├─ Completed:   7
   └─ Failed:      3
 
 Monitor: watch -n 2 cat .loki/STATUS.txt
diff --git a/.loki/app-runner/detection.json b/.loki/app-runner/detection.json
index 84c6984..dba720e 100644
--- a/.loki/app-runner/detection.json
+++ b/.loki/app-runner/detection.json
@@ -3,5 +3,5 @@
     "command": "npm run dev",
     "port": 5173,
     "is_docker": false,
-    "detected_at": "2026-03-21T21:27:35Z"
+    "detected_at": "2026-03-21T21:51:40Z"
 }
diff --git a/.loki/app-runner/state.json b/.loki/app-runner/state.json
index 3759357..93208d8 100644
--- a/.loki/app-runner/state.json
+++ b/.loki/app-runner/state.json
@@ -1,12 +1,12 @@
 {
-    "main_pid": 0,
-    "process_group": "-0",
+    "main_pid": 34053,
+    "process_group": "-34053",
     "method": "npm run dev",
     "port": 5173,
     "url": "http://localhost:5173",
-    "started_at": "2026-03-21T21:29:46Z",
+    "started_at": "2026-03-21T21:51:46Z",
     "restart_count": 1,
-    "status": "stopped",
+    "status": "running",
     "last_health": {"ok": false},
     "crash_count": 0
 }
diff --git a/.loki/autonomy-state.json b/.loki/autonomy-state.json
index 0269f5b..46e58a8 100644
--- a/.loki/autonomy-state.json
+++ b/.loki/autonomy-state.json
@@ -1,10 +1,10 @@
 {
     "retryCount": 2,
-    "status": "completion_promise_fulfilled",
+    "status": "exited",
     "lastExitCode": 0,
-    "lastRun": "2026-03-21T21:29:46Z",
-    "prdPath": "/Users/vishnu/workplace/showtime/openspec/changes/showtime-data-layer-rundown/proposal.md",
-    "pid": 2384,
+    "lastRun": "2026-03-21T21:51:40Z",
+    "prdPath": "/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md",
+    "pid": 65108,
     "maxRetries": 50,
     "baseWait": 60
 }
diff --git a/.loki/context/last_offset.txt b/.loki/context/last_offset.txt
index c227083..e0c1820 100644
--- a/.loki/context/last_offset.txt
+++ b/.loki/context/last_offset.txt
@@ -1 +1 @@
-0
\ No newline at end of file
+244
\ No newline at end of file
diff --git a/.loki/context/tracking.json b/.loki/context/tracking.json
index 8430030..e0eb3f2 100644
--- a/.loki/context/tracking.json
+++ b/.loki/context/tracking.json
@@ -1,15 +1,15 @@
 {
-  "session_id": "75ec375b-f2b5-49fd-9d84-096be420299d",
+  "session_id": "e57fc6e2-9b10-49bc-a529-a3b4785aefd5",
   "provider": "claude",
-  "updated_at": "2026-03-21T21:27:35.875531+00:00",
+  "updated_at": "2026-03-21T21:51:40.271849+00:00",
   "current": {
-    "input_tokens": 0,
-    "output_tokens": 0,
-    "cache_read_tokens": 0,
-    "cache_creation_tokens": 0,
-    "total_tokens": 0,
-    "context_window_pct": 0.0,
-    "estimated_cost_usd": 35.1985
+    "input_tokens": 1,
+    "output_tokens": 36247,
+    "cache_read_tokens": 118290,
+    "cache_creation_tokens": 311203,
+    "total_tokens": 118291,
+    "context_window_pct": 59.15,
+    "estimated_cost_usd": 40.1051
   },
   "compactions": [
     {
@@ -48,9 +48,9 @@
     }
   ],
   "totals": {
-    "total_input": 62390,
-    "total_output": 192616,
-    "total_cost_usd": 35.1985,
+    "total_input": 73189,
+    "total_output": 228863,
+    "total_cost_usd": 40.1051,
     "compaction_count": 2,
     "iterations_tracked": 2
   }
diff --git a/.loki/council/state.json b/.loki/council/state.json
index 3fd1e87..eaaa60b 100644
--- a/.loki/council/state.json
+++ b/.loki/council/state.json
@@ -1,15 +1,12 @@
 {
-  "initialized": true,
-  "enabled": true,
-  "total_votes": 0,
-  "approve_votes": 0,
-  "reject_votes": 0,
-  "last_check_iteration": 0,
-  "consecutive_no_change": 0,
-  "done_signals": 0,
-  "convergence_history": [],
-  "verdicts": [],
-  "total_done_signals": 0,
-  "last_track_iteration": 2,
-  "files_changed": 51
-}
\ No newline at end of file
+    "initialized": true,
+    "enabled": true,
+    "total_votes": 0,
+    "approve_votes": 0,
+    "reject_votes": 0,
+    "last_check_iteration": 0,
+    "consecutive_no_change": 0,
+    "done_signals": 0,
+    "convergence_history": [],
+    "verdicts": []
+}
diff --git a/.loki/dashboard-state.json b/.loki/dashboard-state.json
index 2ee11a0..02ab58b 100644
--- a/.loki/dashboard-state.json
+++ b/.loki/dashboard-state.json
@@ -1,5 +1,5 @@
 {
-  "timestamp": "2026-03-21T21:29:45Z",
+  "timestamp": "2026-03-21T21:51:54Z",
   "version": "unknown",
   "project": {
     "name": "showtime",
@@ -73,6 +73,15 @@
     "completedAt": "2026-03-21T21:27:35Z",
     "exitCode": 0,
     "provider": "claude"
+  },
+  {
+    "id": "iteration-1",
+    "type": "iteration",
+    "title": "Iteration 1",
+    "status": "completed",
+    "completedAt": "2026-03-21T21:51:40Z",
+    "exitCode": 0,
+    "provider": "claude"
   }
 ],
     "failed": [
@@ -109,12 +118,12 @@
   "metrics": {
     "tasksCompleted": 0,
     "tasksFailed": 0,
-    "cpuUsage": 41,
-    "memoryUsage": 67,
+    "cpuUsage": 32,
+    "memoryUsage": 68,
     "resourceStatus": "ok"
   },
   "memory": {
-    "episodic": 8,
+    "episodic": 9,
     "semantic": 2,
     "procedural": 0
   },
@@ -133,31 +142,31 @@
 },
   "checklist": null,
   "appRunner": {
-    "main_pid": 51144,
-    "process_group": "-51144",
+    "main_pid": 34053,
+    "process_group": "-34053",
     "method": "npm run dev",
     "port": 5173,
     "url": "http://localhost:5173",
-    "started_at": "2026-03-21T21:27:39Z",
+    "started_at": "2026-03-21T21:51:46Z",
     "restart_count": 1,
-    "status": "stopped",
+    "status": "running",
     "last_health": {"ok": false},
     "crash_count": 0
 },
   "playwright": null,
   "budget": null,
   "context": {
-  "session_id": "75ec375b-f2b5-49fd-9d84-096be420299d",
+  "session_id": "e57fc6e2-9b10-49bc-a529-a3b4785aefd5",
   "provider": "claude",
-  "updated_at": "2026-03-21T21:27:35.875531+00:00",
+  "updated_at": "2026-03-21T21:51:40.271849+00:00",
   "current": {
-    "input_tokens": 0,
-    "output_tokens": 0,
-    "cache_read_tokens": 0,
-    "cache_creation_tokens": 0,
-    "total_tokens": 0,
-    "context_window_pct": 0.0,
-    "estimated_cost_usd": 35.1985
+    "input_tokens": 1,
+    "output_tokens": 36247,
+    "cache_read_tokens": 118290,
+    "cache_creation_tokens": 311203,
+    "total_tokens": 118291,
+    "context_window_pct": 59.15,
+    "estimated_cost_usd": 40.1051
   },
   "compactions": [
     {
@@ -196,13 +205,13 @@
     }
   ],
   "totals": {
-    "total_input": 62390,
-    "total_output": 192616,
-    "total_cost_usd": 35.1985,
+    "total_input": 73189,
+    "total_output": 228863,
+    "total_cost_usd": 40.1051,
     "compaction_count": 2,
     "iterations_tracked": 2
   }
 },
-  "tokens": {"input": 62390, "output": 192616, "cost_usd": 35.1985},
+  "tokens": {"input": 73189, "output": 228863, "cost_usd": 40.1051},
   "notifications": {"total":0,"unacknowledged":0,"critical":0,"warning":0,"info":0}
 }
diff --git a/.loki/dashboard/dashboard.pid b/.loki/dashboard/dashboard.pid
index 13d7e77..11e83dd 100644
--- a/.loki/dashboard/dashboard.pid
+++ b/.loki/dashboard/dashboard.pid
@@ -1 +1 @@
-2623
+65383
diff --git a/.loki/events.jsonl b/.loki/events.jsonl
index c294e7a..6be5b93 100644
--- a/.loki/events.jsonl
+++ b/.loki/events.jsonl
@@ -48,3 +48,6 @@
 {"timestamp":"2026-03-21T21:27:42Z","type":"code_review_start","data":{"review_id":"review-20260321T212742Z-2","reviewers":"architecture-strategist, test-coverage-auditor, performance-oracle","iteration":2}}
 {"timestamp":"2026-03-21T21:29:45Z","type":"code_review_complete","data":{"review_id":"review-20260321T212742Z-2","pass_count":3,"fail_count":0,"has_blocking":false,"iteration":2}}
 {"timestamp":"2026-03-21T21:29:46Z","type":"session_end","data":{"result":0,"provider":"claude","iterations":2}}
+{"timestamp":"2026-03-21T21:36:17Z","type":"session_start","data":{"provider":"claude","prd":"/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md","parallel":true,"complexity":"standard","pid":65108}}
+{"timestamp":"2026-03-21T21:36:17Z","type":"iteration_start","data":{"iteration":1,"provider":"claude","prd":"/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md"}}
+{"timestamp":"2026-03-21T21:51:40Z","type":"iteration_complete","data":{"iteration":1,"status":"completed","exitCode":0,"provider":"claude"}}
diff --git a/.loki/logs/agent-audit.jsonl b/.loki/logs/agent-audit.jsonl
index 3330e91..c5db03d 100644
--- a/.loki/logs/agent-audit.jsonl
+++ b/.loki/logs/agent-audit.jsonl
@@ -21,3 +21,5 @@
 {"timestamp": "2026-03-21T20:47:03Z", "action": "cli_invoke", "description": "Starting iteration 1", "details": "provider=claude,tier=development", "iteration": 1, "pid": 2384}
 {"timestamp": "2026-03-21T21:20:19Z", "action": "cli_invoke", "description": "Starting iteration 2", "details": "provider=claude,tier=development", "iteration": 2, "pid": 2384}
 {"timestamp": "2026-03-21T21:29:46Z", "action": "session_stop", "description": "Session ended", "details": "result=0,iterations=2", "iteration": 2, "pid": 2384}
+{"timestamp": "2026-03-21T21:36:17Z", "action": "session_start", "description": "Session started", "details": "prd=/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md,provider=claude", "iteration": 0, "pid": 65108}
+{"timestamp": "2026-03-21T21:36:18Z", "action": "cli_invoke", "description": "Starting iteration 1", "details": "provider=claude,tier=development", "iteration": 1, "pid": 65108}
diff --git a/.loki/logs/audit-20260321.jsonl b/.loki/logs/audit-20260321.jsonl
index 5bb31d9..f2ce55e 100644
--- a/.loki/logs/audit-20260321.jsonl
+++ b/.loki/logs/audit-20260321.jsonl
@@ -54,3 +54,10 @@
   "user": "vishnu",
   "pid": 2384
 }
+{
+  "timestamp": "2026-03-21T21:36:17Z",
+  "event": "SESSION_START",
+  "data": "prd=/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md,dashboard=true,staged_autonomy=false,parallel=true",
+  "user": "vishnu",
+  "pid": 65108
+}
diff --git a/.loki/metrics/efficiency/iteration-1.json b/.loki/metrics/efficiency/iteration-1.json
index 12e8574..a353ad2 100644
--- a/.loki/metrics/efficiency/iteration-1.json
+++ b/.loki/metrics/efficiency/iteration-1.json
@@ -2,11 +2,11 @@
   "iteration": 1,
   "model": "opus",
   "phase": "DEVELOPMENT",
-  "duration_ms": 1710000,
+  "duration_ms": 923000,
   "provider": "claude",
-  "status": "failed",
+  "status": "completed",
   "input_tokens": 2891,
   "output_tokens": 80165,
   "cost_usd": 13.4582,
-  "timestamp": "2026-03-21T21:15:33Z"
+  "timestamp": "2026-03-21T21:51:40Z"
 }
diff --git a/.loki/metrics/efficiency/iteration-2.json b/.loki/metrics/efficiency/iteration-2.json
deleted file mode 100644
index 28887bd..0000000
--- a/.loki/metrics/efficiency/iteration-2.json
+++ /dev/null
@@ -1,12 +0,0 @@
-{
-  "iteration": 2,
-  "model": "opus",
-  "phase": "DEVELOPMENT",
-  "duration_ms": 437000,
-  "provider": "claude",
-  "status": "completed",
-  "input_tokens": 0,
-  "output_tokens": 0,
-  "cost_usd": 0.0,
-  "timestamp": "2026-03-21T21:27:35Z"
-}
diff --git a/.loki/pids/2623.json b/.loki/pids/2623.json
deleted file mode 100644
index 3aac710..0000000
--- a/.loki/pids/2623.json
+++ /dev/null
@@ -1 +0,0 @@
-{"pid":2623,"label":"dashboard","started":"2026-03-21T20:46:57Z","ppid":2384,"extra":"port=57374"}
diff --git a/.loki/prd-observations.md b/.loki/prd-observations.md
index a86969f..016b459 100644
--- a/.loki/prd-observations.md
+++ b/.loki/prd-observations.md
@@ -1,29 +1,29 @@
 # PRD Analysis Observations
 
-**Source:** `/Users/vishnu/workplace/showtime/openspec/changes/showtime-data-layer-rundown/proposal.md`
-**Analyzed:** 2026-03-21T20:47:03Z
-**Quality Score:** 3.6/10
-**Estimated Scope:** enterprise (~32 items detected)
+**Source:** `/Users/vishnu/workplace/showtime/openspec/changes/showtime-audio-polish/proposal.md`
+**Analyzed:** 2026-03-21T21:36:17Z
+**Quality Score:** 2.9/10
+**Estimated Scope:** medium (~6 items detected)
 
 ## Strengths
 
 - **Feature List**: Partially covered (mentioned but lacks dedicated section)
 - **Tech Stack**: Partially covered (mentioned but lacks dedicated section)
 - **User Stories / Flows**: Partially covered (mentioned but lacks dedicated section)
-- **Acceptance Criteria**: Partially covered (mentioned but lacks dedicated section)
-- **Data Model / Schema**: Well-defined with dedicated section and content
+- **Data Model / Schema**: Partially covered (mentioned but lacks dedicated section)
+- **Deployment Requirements**: Partially covered (mentioned but lacks dedicated section)
 
 ## Missing or Unclear
 
+- **Acceptance Criteria**: Measurable completion criteria or checklists
 - **API Specifications**: API endpoints, request/response formats
-- **Deployment Requirements**: Deployment targets, CI/CD, and infrastructure
 - **Error Handling**: Error handling, edge cases, and failure modes
 - **Security Requirements**: Authentication, authorization, and security measures
 
 ## Assumptions Made
 
+- Will generate acceptance criteria from requirements
 - Will define API endpoints based on feature set
-- Will use standard containerized deployment
 - Will implement standard error handling patterns
 - Will apply baseline security (input validation, auth if applicable)
 
@@ -32,8 +32,9 @@
 - Add a dedicated **Feature List** section with more detail
 - Add a dedicated **Tech Stack** section with more detail
 - Add a dedicated **User Stories / Flows** section with more detail
-- Add a dedicated **Acceptance Criteria** section with more detail
+- Add **Acceptance Criteria** section: Measurable completion criteria or checklists
+- Add a dedicated **Data Model / Schema** section with more detail
 - Add **API Specifications** section: API endpoints, request/response formats
-- Add **Deployment Requirements** section: Deployment targets, CI/CD, and infrastructure
+- Add a dedicated **Deployment Requirements** section with more detail
 - Add **Error Handling** section: Error handling, edge cases, and failure modes
 - Add **Security Requirements** section: Authentication, authorization, and security measures
diff --git a/.loki/quality/gate-failure-count.json b/.loki/quality/gate-failure-count.json
index 01a5e42..e7e7a0f 100644
--- a/.loki/quality/gate-failure-count.json
+++ b/.loki/quality/gate-failure-count.json
@@ -1,5 +1,5 @@
 {
-  "static_analysis": 4,
+  "static_analysis": 5,
   "test_coverage": 0,
   "code_review": 0
 }
\ No newline at end of file
diff --git a/.loki/quality/static-analysis.json b/.loki/quality/static-analysis.json
index edef861..2bee65d 100644
--- a/.loki/quality/static-analysis.json
+++ b/.loki/quality/static-analysis.json
@@ -1 +1 @@
-{"timestamp":"2026-03-21T21:27:40Z","files_checked":1,"findings":1,"summary":"Syntax error: /Users/vishnu/workplace/showtime/src/renderer/components/RundownBar.tsx. ","pass":false}
+{"timestamp":"2026-03-21T21:51:48Z","files_checked":11,"findings":9,"summary":"Syntax error: /Users/vishnu/workplace/showtime/src/__tests__/audioAndPolish.test.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/components/BeatCheckModal.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/components/IntermissionView.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/components/MuteToggle.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/views/ExpandedView.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/views/GoingLiveTransition.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/views/PillView.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/views/StrikeView.tsx. Syntax error: /Users/vishnu/workplace/showtime/src/renderer/views/WritersRoomView.tsx. ","pass":false}
diff --git a/.loki/quality/test-results.json b/.loki/quality/test-results.json
index a3ad785..0cd960d 100644
--- a/.loki/quality/test-results.json
+++ b/.loki/quality/test-results.json
@@ -1 +1 @@
-{"timestamp":"2026-03-21T21:27:42Z","runner":"vitest","pass":true,"min_coverage":80,"summary":"vitest: {'numTotalTestSuites':61,'numPassedTestSuites':61,'numFailedTestSuites':0,'numPendingTestSuites':0,'numTotalTests':176,'numPassedTests':176,'numFailedTests':0,'numPendingTests':0,'numTodoTests':0,'snapshot':{'added':0,'failure':false,'filesAdded':0,'filesRemoved':0,'filesRemovedList':[],'filesUnmatched':0,'filesUpdated':0,'matched':0,'total':0,'unchecked':0,'uncheckedKeysByFile':[],'unmatched':0,'updated':0,'didUpdate':false},'startTime':1774128461095,'success':true,'testResults':[{'asse"}
+{"timestamp":"2026-03-21T21:51:56Z","runner":"vitest","pass":true,"min_coverage":80,"summary":"vitest: {'numTotalTestSuites':67,'numPassedTestSuites':67,'numFailedTestSuites':0,'numPendingTestSuites':0,'numTotalTests':186,'numPassedTests':186,'numFailedTests':0,'numPendingTests':0,'numTodoTests':0,'snapshot':{'added':0,'failure':false,'filesAdded':0,'filesRemoved':0,'filesRemovedList':[],'filesUnmatched':0,'filesUpdated':0,'matched':0,'total':0,'unchecked':0,'uncheckedKeysByFile':[],'unmatched':0,'updated':0,'didUpdate':false},'startTime':1774129910044,'success':true,'testResults':[{'asse"}
diff --git a/.loki/queue/completed.json b/.loki/queue/completed.json
index dd024e2..69d8b66 100644
--- a/.loki/queue/completed.json
+++ b/.loki/queue/completed.json
@@ -52,5 +52,14 @@
     "completedAt": "2026-03-21T21:27:35Z",
     "exitCode": 0,
     "provider": "claude"
+  },
+  {
+    "id": "iteration-1",
+    "type": "iteration",
+    "title": "Iteration 1",
+    "status": "completed",
+    "completedAt": "2026-03-21T21:51:40Z",
+    "exitCode": 0,
+    "provider": "claude"
   }
 ]
\ No newline at end of file
diff --git a/.loki/state/checkpoints/index.jsonl b/.loki/state/checkpoints/index.jsonl
index 0ae8728..abd1772 100644
--- a/.loki/state/checkpoints/index.jsonl
+++ b/.loki/state/checkpoints/index.jsonl
@@ -16,3 +16,4 @@
 {"id": "cp-1-1774127868", "ts": "2026-03-21T21:17:48Z", "iter": 1, "task": "iteration-1 failed (exit=141)", "sha": "eaba5e9472fe1e477c0491606e17b6b617f853d7"}
 {"id": "cp-2-1774128460", "ts": "2026-03-21T21:27:40Z", "iter": 2, "task": "iteration-2 complete", "sha": "056cf9343b1380396656172eec53fc1972927234"}
 {"id": "cp-2-1774128586", "ts": "2026-03-21T21:29:46Z", "iter": 2, "task": "session end (iterations=2)", "sha": "056cf9343b1380396656172eec53fc1972927234"}
+{"id": "cp-1-1774129907", "ts": "2026-03-21T21:51:47Z", "iter": 1, "task": "iteration-1 complete", "sha": "3e1bd208a80044b77d8808a689aeb304548d0c68"}
diff --git a/.loki/state/resources.json b/.loki/state/resources.json
index 07c69bf..3244653 100644
--- a/.loki/state/resources.json
+++ b/.loki/state/resources.json
@@ -1,12 +1,12 @@
 {
-  "timestamp": "2026-03-21T21:27:23Z",
+  "timestamp": "2026-03-21T21:51:24Z",
   "cpu": {
-    "usage_percent": 41,
+    "usage_percent": 32,
     "threshold_percent": 80,
     "status": "ok"
   },
   "memory": {
-    "usage_percent": 67,
+    "usage_percent": 68,
     "threshold_percent": 80,
     "status": "ok"
   },
diff --git a/src/__tests__/audioAndPolish.test.tsx b/src/__tests__/audioAndPolish.test.tsx
new file mode 100644
index 0000000..ccfab1d
--- /dev/null
+++ b/src/__tests__/audioAndPolish.test.tsx
@@ -0,0 +1,250 @@
+import { describe, it, expect, vi, beforeAll, beforeEach, afterEach } from 'vitest'
+import { render, screen, fireEvent, cleanup } from '@testing-library/react'
+import React from 'react'
+import { useShowStore } from '../renderer/stores/showStore'
+
+// ─── Mock framer-motion ───
+vi.mock('framer-motion', () => ({
+  motion: {
+    div: React.forwardRef((props: any, ref: any) => <div ref={ref} {...stripMotionProps(props)} />),
+    button: React.forwardRef((props: any, ref: any) => <button ref={ref} {...stripMotionProps(props)} />),
+    h1: React.forwardRef((props: any, ref: any) => <h1 ref={ref} {...stripMotionProps(props)} />),
+    h2: React.forwardRef((props: any, ref: any) => <h2 ref={ref} {...stripMotionProps(props)} />),
+    p: React.forwardRef((props: any, ref: any) => <p ref={ref} {...stripMotionProps(props)} />),
+    span: React.forwardRef((props: any, ref: any) => <span ref={ref} {...stripMotionProps(props)} />),
+  },
+  AnimatePresence: ({ children }: any) => <>{children}</>,
+}))
+
+// Mock audio module to prevent Web Audio API calls in tests
+vi.mock('../renderer/hooks/useAudio', () => ({
+  playAudioCue: vi.fn(),
+  isMuted: vi.fn(() => false),
+  setMuted: vi.fn(),
+  useAudio: () => ({
+    play: vi.fn(),
+    resetTimerWarning: vi.fn(),
+    playTimerWarningOnce: vi.fn(),
+  }),
+}))
+
+function stripMotionProps(props: any) {
+  const {
+    initial, animate, exit, transition, whileHover, whileTap, whileFocus,
+    layout, layoutId, variants, onAnimationStart, onAnimationComplete,
+    ...rest
+  } = props
+  return rest
+}
+
+// Set up window.clui mock (don't replace the whole window object)
+beforeAll(() => {
+  ;(window as any).clui = {
+    quit: vi.fn(),
+    setViewMode: vi.fn(),
+    dataSync: vi.fn(),
+    dataFlush: vi.fn(),
+    timelineRecord: vi.fn(),
+    getTimelineDrift: vi.fn().mockResolvedValue(0),
+  }
+})
+
+function resetStore() {
+  useShowStore.setState({
+    phase: 'no_show',
+    energy: null,
+    acts: [],
+    currentActId: null,
+    beatsLocked: 0,
+    beatThreshold: 3,
+    timerEndAt: null,
+    timerPausedRemaining: null,
+    claudeSessionId: null,
+    showDate: new Date().toISOString().slice(0, 10),
+    showStartedAt: null,
+    verdict: null,
+    isExpanded: true,
+    beatCheckPending: false,
+    celebrationActive: false,
+    goingLiveActive: false,
+    writersRoomStep: 'energy',
+    writersRoomEnteredAt: null,
+    breathingPauseEndAt: null,
+  })
+}
+
+beforeEach(() => {
+  resetStore()
+  cleanup()
+  localStorage.clear()
+})
+
+afterEach(() => {
+  cleanup()
+})
+
+// ─── useAudio API Tests (non-mocked, test the module interface) ───
+
+describe('useAudio module interface', () => {
+  it('playAudioCue is callable', async () => {
+    // Since we mock useAudio, verify the mock is callable
+    const { playAudioCue } = await import('../renderer/hooks/useAudio')
+    expect(typeof playAudioCue).toBe('function')
+    expect(() => playAudioCue('going-live')).not.toThrow()
+  })
+
+  it('isMuted and setMuted are callable', async () => {
+    const { isMuted, setMuted } = await import('../renderer/hooks/useAudio')
+    expect(typeof isMuted).toBe('function')
+    expect(typeof setMuted).toBe('function')
+  })
+})
+
+// ─── MuteToggle Tests ───
+
+describe('MuteToggle', () => {
+  it('renders a toggle button', async () => {
+    // MuteToggle uses the real isMuted/setMuted but those are mocked
+    const { MuteToggle } = await import('../renderer/components/MuteToggle')
+    render(<MuteToggle />)
+    const btn = screen.getByTestId('mute-toggle')
+    expect(btn).toBeInTheDocument()
+  })
+})
+
+// ─── PillView Drag Zone Tests ───
+
+describe('PillView drag zones', () => {
+  let PillView: any
+
+  beforeEach(async () => {
+    const mod = await import('../renderer/views/PillView')
+    PillView = mod.PillView
+  })
+
+  it('renders drag zone and click zone when live', () => {
+    useShowStore.setState({
+      phase: 'live',
+      acts: [{ id: 'a1', name: 'Deep Work', sketch: 'Deep Work', durationMinutes: 30, status: 'active', beatLocked: false, order: 0, startedAt: Date.now() }],
+      currentActId: 'a1',
+      timerEndAt: Date.now() + 30 * 60 * 1000,
+      isExpanded: false,
+    })
+    const { container } = render(<PillView />)
+    const dragZone = container.querySelector('.drag-region')
+    const noDragZone = container.querySelector('.no-drag')
+    expect(dragZone).toBeInTheDocument()
+    expect(noDragZone).toBeInTheDocument()
+  })
+
+  it('click zone triggers toggleExpanded', () => {
+    const toggleSpy = vi.fn()
+    useShowStore.setState({
+      phase: 'live',
+      acts: [{ id: 'a1', name: 'Deep Work', sketch: 'Deep Work', durationMinutes: 30, status: 'active', beatLocked: false, order: 0, startedAt: Date.now() }],
+      currentActId: 'a1',
+      timerEndAt: Date.now() + 30 * 60 * 1000,
+      isExpanded: false,
+      toggleExpanded: toggleSpy,
+    })
+    const { container } = render(<PillView />)
+    const noDragZone = container.querySelector('.no-drag')
+    expect(noDragZone).toBeInTheDocument()
+    fireEvent.click(noDragZone!)
+    expect(toggleSpy).toHaveBeenCalled()
+  })
+})
+
+// ─── StrikeView Celebration Tests ───
+
+describe('StrikeView celebration', () => {
+  let StrikeView: any
+
+  beforeEach(async () => {
+    const mod = await import('../renderer/views/StrikeView')
+    StrikeView = mod.StrikeView
+  })
+
+  it('renders celebration buttons', () => {
+    useShowStore.setState({
+      phase: 'strike',
+      verdict: 'SOLID_SHOW' as any,
+      acts: [{ id: 'a1', name: 'Work', sketch: 'Deep Work', durationMinutes: 30, status: 'completed', beatLocked: true, order: 0, startedAt: Date.now() - 30 * 60 * 1000, completedAt: Date.now() }],
+      beatsLocked: 1,
+      beatThreshold: 3,
+      showStartedAt: Date.now() - 60 * 60 * 1000,
+      isExpanded: true,
+    })
+    render(<StrikeView />)
+    expect(screen.getByTestId('encore-btn')).toBeInTheDocument()
+    expect(screen.getByTestId('plan-tomorrow-btn')).toBeInTheDocument()
+    expect(screen.getByTestId('thats-a-wrap-btn')).toBeInTheDocument()
+  })
+
+  it('shows show duration stat', () => {
+    useShowStore.setState({
+      phase: 'strike',
+      verdict: 'SOLID_SHOW' as any,
+      acts: [],
+      beatsLocked: 0,
+      beatThreshold: 3,
+      showStartedAt: Date.now() - 2 * 60 * 60 * 1000,
+      isExpanded: true,
+    })
+    render(<StrikeView />)
+    const duration = screen.getByTestId('show-duration')
+    expect(duration).toBeInTheDocument()
+    expect(duration.textContent).toContain('2h')
+  })
+
+  it('shows standing ovation for DAY_WON', () => {
+    useShowStore.setState({
+      phase: 'strike',
+      verdict: 'DAY_WON' as any,
+      acts: [],
+      beatsLocked: 3,
+      beatThreshold: 3,
+      showStartedAt: Date.now() - 60 * 60 * 1000,
+      isExpanded: true,
+    })
+    render(<StrikeView />)
+    expect(screen.getByText('Standing ovation.')).toBeInTheDocument()
+  })
+})
+
+// ─── Temporal Context Tests ───
+
+describe('Temporal context', () => {
+  it('ExpandedView shows date label', async () => {
+    const { ExpandedView } = await import('../renderer/views/ExpandedView')
+    useShowStore.setState({
+      phase: 'live',
+      acts: [{ id: 'a1', name: 'Work', sketch: 'Deep Work', durationMinutes: 30, status: 'active', beatLocked: false, order: 0, startedAt: Date.now() }],
+      currentActId: 'a1',
+      timerEndAt: Date.now() + 30 * 60 * 1000,
+      showStartedAt: Date.now(),
+      isExpanded: true,
+    })
+    render(<ExpandedView />)
+    const dateLabel = screen.getByTestId('date-label')
+    expect(dateLabel).toBeInTheDocument()
+    // Format: "SAT, MAR 21" or "SAT MAR 21" — uppercase abbreviated date
+    expect(dateLabel.textContent).toMatch(/^[A-Z]{3}/)
+  })
+
+  it('ExpandedView shows started-at time', async () => {
+    const { ExpandedView } = await import('../renderer/views/ExpandedView')
+    useShowStore.setState({
+      phase: 'live',
+      acts: [{ id: 'a1', name: 'Work', sketch: 'Deep Work', durationMinutes: 30, status: 'active', beatLocked: false, order: 0, startedAt: Date.now() }],
+      currentActId: 'a1',
+      timerEndAt: Date.now() + 30 * 60 * 1000,
+      showStartedAt: Date.now(),
+      isExpanded: true,
+    })
+    render(<ExpandedView />)
+    const startedAt = screen.getByTestId('started-at')
+    expect(startedAt).toBeInTheDocument()
+    expect(startedAt.textContent).toContain('Started')
+  })
+})
diff --git a/src/renderer/components/BeatCheckModal.tsx b/src/renderer/components/BeatCheckModal.tsx
index eb1c503..2c77376 100644
--- a/src/renderer/components/BeatCheckModal.tsx
+++ b/src/renderer/components/BeatCheckModal.tsx
@@ -1,7 +1,9 @@
+import { useEffect, useRef } from 'react'
 import { motion, AnimatePresence } from 'framer-motion'
 import { useShowStore, selectCurrentAct } from '../stores/showStore'
 import { ClapperboardBadge } from './ClapperboardBadge'
 import { Button } from '../ui/button'
+import { playAudioCue } from '../hooks/useAudio'
 
 export function BeatCheckModal() {
   const beatCheckPending = useShowStore((s) => s.beatCheckPending)
@@ -11,6 +13,15 @@ export function BeatCheckModal() {
   const acts = useShowStore((s) => s.acts)
   const currentAct = useShowStore(selectCurrentAct)
 
+  // Play beat-check audio when modal appears
+  const prevPending = useRef(false)
+  useEffect(() => {
+    if (beatCheckPending && !prevPending.current) {
+      playAudioCue('beat-check')
+    }
+    prevPending.current = beatCheckPending
+  }, [beatCheckPending])
+
   // Show modal when Beat Check is pending OR celebration is playing
   if (!beatCheckPending && !celebrationActive) return null
 
@@ -19,6 +30,7 @@ export function BeatCheckModal() {
     : 0
 
   const handleLockBeat = () => {
+    playAudioCue('beat-locked')
     lockBeat()
   }
 
diff --git a/src/renderer/components/IntermissionView.tsx b/src/renderer/components/IntermissionView.tsx
index 8b63f6b..754f45a 100644
--- a/src/renderer/components/IntermissionView.tsx
+++ b/src/renderer/components/IntermissionView.tsx
@@ -3,6 +3,7 @@ import { motion } from 'framer-motion'
 import { useShowStore } from '../stores/showStore'
 import { Button } from '../ui/button'
 import { BeatCounter } from './BeatCounter'
+import { playAudioCue } from '../hooks/useAudio'
 
 const AFFIRMATIONS = [
   'Rest is free. Always has been.',
@@ -18,6 +19,11 @@ export function IntermissionView() {
     () => AFFIRMATIONS[Math.floor(Math.random() * AFFIRMATIONS.length)]
   )
 
+  // Play intermission audio cue once on mount
+  useEffect(() => {
+    playAudioCue('intermission')
+  }, [])
+
   return (
     <motion.div
       className="max-w-[380px] w-full p-8 rounded-xl bg-surface border border-card-border flex flex-col items-center text-center"
diff --git a/src/renderer/components/MuteToggle.tsx b/src/renderer/components/MuteToggle.tsx
new file mode 100644
index 0000000..009ed78
--- /dev/null
+++ b/src/renderer/components/MuteToggle.tsx
@@ -0,0 +1,36 @@
+import { useState, useCallback } from 'react'
+import { isMuted, setMuted } from '../hooks/useAudio'
+
+export function MuteToggle() {
+  const [muted, setMutedState] = useState(isMuted)
+
+  const toggle = useCallback(() => {
+    const next = !muted
+    setMuted(next)
+    setMutedState(next)
+  }, [muted])
+
+  return (
+    <button
+      onClick={toggle}
+      className="px-2 py-1.5 text-txt-muted hover:text-txt-secondary transition-colors no-drag"
+      title={muted ? 'Unmute audio' : 'Mute audio'}
+      data-testid="mute-toggle"
+    >
+      {muted ? (
+        // Speaker with X (muted)
+        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" className="inline-block">
+          <path d="M8 2L4 5.5H1v5h3L8 14V2z" fill="currentColor" />
+          <path d="M12 5.5l3 5M15 5.5l-3 5" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
+        </svg>
+      ) : (
+        // Speaker (unmuted)
+        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" className="inline-block">
+          <path d="M8 2L4 5.5H1v5h3L8 14V2z" fill="currentColor" />
+          <path d="M11 5.5a3.5 3.5 0 010 5" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
+          <path d="M13 3.5a6.5 6.5 0 010 9" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
+        </svg>
+      )}
+    </button>
+  )
+}
diff --git a/src/renderer/hooks/useAudio.ts b/src/renderer/hooks/useAudio.ts
new file mode 100644
index 0000000..3b83577
--- /dev/null
+++ b/src/renderer/hooks/useAudio.ts
@@ -0,0 +1,143 @@
+import { useCallback, useRef, useEffect } from 'react'
+
+export type AudioCue =
+  | 'going-live'
+  | 'beat-check'
+  | 'beat-locked'
+  | 'timer-warning'
+  | 'show-complete'
+  | 'intermission'
+
+const MUTE_KEY = 'showtime-audio-muted'
+
+let audioContext: AudioContext | null = null
+
+function getAudioContext(): AudioContext {
+  if (!audioContext) {
+    audioContext = new AudioContext()
+  }
+  if (audioContext.state === 'suspended') {
+    audioContext.resume()
+  }
+  return audioContext
+}
+
+function playTone(
+  ctx: AudioContext,
+  frequency: number,
+  duration: number,
+  startTime: number,
+  type: OscillatorType = 'sine',
+  gain = 0.15,
+) {
+  const osc = ctx.createOscillator()
+  const gainNode = ctx.createGain()
+  osc.type = type
+  osc.frequency.setValueAtTime(frequency, startTime)
+  gainNode.gain.setValueAtTime(gain, startTime)
+  gainNode.gain.exponentialRampToValueAtTime(0.001, startTime + duration)
+  osc.connect(gainNode)
+  gainNode.connect(ctx.destination)
+  osc.start(startTime)
+  osc.stop(startTime + duration)
+}
+
+function playChord(
+  ctx: AudioContext,
+  frequencies: number[],
+  duration: number,
+  startTime: number,
+  type: OscillatorType = 'sine',
+  gain = 0.1,
+) {
+  for (const freq of frequencies) {
+    playTone(ctx, freq, duration, startTime, type, gain)
+  }
+}
+
+const CUE_PLAYERS: Record<AudioCue, (ctx: AudioContext) => void> = {
+  // Ascending two-tone chime (C5 → G5)
+  'going-live': (ctx) => {
+    const now = ctx.currentTime
+    playTone(ctx, 523.25, 0.25, now, 'sine', 0.2) // C5
+    playTone(ctx, 783.99, 0.35, now + 0.2, 'sine', 0.2) // G5
+  },
+
+  // Single warm bell (A4)
+  'beat-check': (ctx) => {
+    const now = ctx.currentTime
+    playTone(ctx, 440, 0.6, now, 'sine', 0.15)
+    playTone(ctx, 880, 0.4, now, 'sine', 0.05) // harmonic
+  },
+
+  // Confirmation chord (C4+E4+G4)
+  'beat-locked': (ctx) => {
+    const now = ctx.currentTime
+    playChord(ctx, [261.63, 329.63, 392.0], 0.4, now, 'sine', 0.1)
+  },
+
+  // Soft tick (filtered noise-like)
+  'timer-warning': (ctx) => {
+    const now = ctx.currentTime
+    playTone(ctx, 1000, 0.08, now, 'square', 0.06)
+  },
+
+  // Ascending arpeggio (C4→E4→G4→C5)
+  'show-complete': (ctx) => {
+    const now = ctx.currentTime
+    const notes = [261.63, 329.63, 392.0, 523.25]
+    notes.forEach((freq, i) => {
+      playTone(ctx, freq, 0.3, now + i * 0.15, 'sine', 0.15)
+    })
+  },
+
+  // Descending soft tone (G4→C4)
+  'intermission': (ctx) => {
+    const now = ctx.currentTime
+    playTone(ctx, 392.0, 0.35, now, 'sine', 0.12)
+    playTone(ctx, 261.63, 0.45, now + 0.3, 'sine', 0.12)
+  },
+}
+
+export function isMuted(): boolean {
+  try {
+    return localStorage.getItem(MUTE_KEY) === 'true'
+  } catch {
+    return false
+  }
+}
+
+export function setMuted(muted: boolean): void {
+  try {
+    localStorage.setItem(MUTE_KEY, String(muted))
+  } catch { /* ignore */ }
+}
+
+export function playAudioCue(cue: AudioCue): void {
+  if (isMuted()) return
+  try {
+    const ctx = getAudioContext()
+    CUE_PLAYERS[cue](ctx)
+  } catch { /* audio is non-critical */ }
+}
+
+export function useAudio() {
+  const timerWarningFired = useRef(false)
+
+  const play = useCallback((cue: AudioCue) => {
+    playAudioCue(cue)
+  }, [])
+
+  const resetTimerWarning = useCallback(() => {
+    timerWarningFired.current = false
+  }, [])
+
+  const playTimerWarningOnce = useCallback(() => {
+    if (!timerWarningFired.current) {
+      timerWarningFired.current = true
+      playAudioCue('timer-warning')
+    }
+  }, [])
+
+  return { play, resetTimerWarning, playTimerWarningOnce }
+}
diff --git a/src/renderer/hooks/useTimer.ts b/src/renderer/hooks/useTimer.ts
index 77ccf88..dfe86da 100644
--- a/src/renderer/hooks/useTimer.ts
+++ b/src/renderer/hooks/useTimer.ts
@@ -1,5 +1,6 @@
-import { useState, useEffect, useCallback } from 'react'
+import { useState, useEffect, useCallback, useRef } from 'react'
 import { useShowStore } from '../stores/showStore'
+import { playAudioCue } from './useAudio'
 
 interface TimerState {
   minutes: number
@@ -19,6 +20,9 @@ export function useTimer(): TimerState {
   const currentAct = acts.find((a) => a.id === currentActId)
   const totalDurationMs = currentAct ? currentAct.durationMinutes * 60 * 1000 : 0
 
+  // Track whether we've fired the 5-minute warning for this act
+  const warningFired = useRef<string | null>(null)
+
   const calcRemaining = useCallback(() => {
     if (!timerEndAt) return 0
     return Math.max(0, timerEndAt - Date.now())
@@ -26,6 +30,11 @@ export function useTimer(): TimerState {
 
   const [remaining, setRemaining] = useState(calcRemaining)
 
+  // Reset warning when act changes
+  useEffect(() => {
+    warningFired.current = null
+  }, [currentActId])
+
   useEffect(() => {
     setRemaining(calcRemaining())
 
@@ -35,6 +44,13 @@ export function useTimer(): TimerState {
       const r = calcRemaining()
       setRemaining(r)
 
+      // Play timer warning once when crossing the 5-minute threshold
+      const mins = Math.ceil(r / 1000 / 60)
+      if (mins <= 5 && mins > 0 && r > 0 && warningFired.current !== currentActId) {
+        warningFired.current = currentActId
+        playAudioCue('timer-warning')
+      }
+
       if (r <= 0 && currentActId) {
         clearInterval(interval)
         completeAct(currentActId)
diff --git a/src/renderer/index.css b/src/renderer/index.css
index af3feb0..8574503 100644
--- a/src/renderer/index.css
+++ b/src/renderer/index.css
@@ -279,3 +279,20 @@ textarea:focus {
     rgba(255,255,255,0.15) 6px
   );
 }
+
+/* ─── Confetti Animation ─── */
+
+@keyframes confetti-fall {
+  0% {
+    transform: translateY(-20px) rotate(0deg);
+    opacity: 1;
+  }
+  100% {
+    transform: translateY(700px) rotate(720deg);
+    opacity: 0;
+  }
+}
+
+.animate-confetti {
+  animation: confetti-fall 3s ease-in forwards;
+}
diff --git a/src/renderer/views/ExpandedView.tsx b/src/renderer/views/ExpandedView.tsx
index 747a5e2..4bbd6a1 100644
--- a/src/renderer/views/ExpandedView.tsx
+++ b/src/renderer/views/ExpandedView.tsx
@@ -1,3 +1,4 @@
+import { useMemo } from 'react'
 import { motion } from 'framer-motion'
 import { useShowStore, selectCurrentAct } from '../stores/showStore'
 import { TimerPanel } from '../panels/TimerPanel'
@@ -7,6 +8,21 @@ import { BeatCounter } from '../components/BeatCounter'
 import { IntermissionView } from '../components/IntermissionView'
 import { DirectorMode } from '../components/DirectorMode'
 import { RundownBar } from '../components/RundownBar'
+import { MuteToggle } from '../components/MuteToggle'
+
+function formatDateLabel(): string {
+  return new Date()
+    .toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' })
+    .toUpperCase()
+}
+
+function formatStartTime(timestamp: number): string {
+  return new Date(timestamp).toLocaleTimeString('en-US', {
+    hour: 'numeric',
+    minute: '2-digit',
+    hour12: true,
+  })
+}
 
 export function ExpandedView() {
   const phase = useShowStore((s) => s.phase)
@@ -16,6 +32,9 @@ export function ExpandedView() {
   const beatThreshold = useShowStore((s) => s.beatThreshold)
   const toggleExpanded = useShowStore((s) => s.toggleExpanded)
   const enterDirector = useShowStore((s) => s.enterDirector)
+  const showStartedAt = useShowStore((s) => s.showStartedAt)
+
+  const dateLabel = useMemo(formatDateLabel, [])
 
   return (
     <motion.div
@@ -29,11 +48,17 @@ export function ExpandedView() {
       <div
         className="bg-[#151517] px-5 py-3 flex items-center justify-between border-b border-[#242428] drag-region"
       >
-        <span className="font-mono text-xs tracking-widest uppercase text-txt-muted">
-          SHOWTIME
-        </span>
+        <div className="flex items-center gap-3">
+          <span className="font-mono text-xs tracking-widest uppercase text-txt-muted">
+            SHOWTIME
+          </span>
+          <span className="font-mono text-[10px] tracking-wider text-txt-muted/60" data-testid="date-label">
+            {dateLabel}
+          </span>
+        </div>
 
         <div className="flex items-center gap-1">
+          <MuteToggle />
           <button
             onClick={enterDirector}
             className="px-3 py-1.5 rounded-lg bg-surface-hover text-txt-secondary text-sm font-medium hover:text-txt-primary transition-colors no-drag"
@@ -78,7 +103,14 @@ export function ExpandedView() {
 
       {/* Bottom Bar */}
       <div className="bg-[#151517] px-5 py-3 flex items-center justify-between border-t border-[#242428]">
-        <OnAirIndicator isLive={phase === 'live'} />
+        <div className="flex items-center gap-3">
+          <OnAirIndicator isLive={phase === 'live'} />
+          {showStartedAt && (
+            <span className="font-mono text-[10px] text-txt-muted" data-testid="started-at">
+              Started {formatStartTime(showStartedAt)}
+            </span>
+          )}
+        </div>
         <BeatCounter size="sm" />
       </div>
 
diff --git a/src/renderer/views/GoingLiveTransition.tsx b/src/renderer/views/GoingLiveTransition.tsx
index 1d97003..aedf8eb 100644
--- a/src/renderer/views/GoingLiveTransition.tsx
+++ b/src/renderer/views/GoingLiveTransition.tsx
@@ -1,6 +1,7 @@
 import { useEffect } from 'react';
 import { motion } from 'framer-motion';
 import { OnAirIndicator } from '../components/OnAirIndicator';
+import { playAudioCue } from '../hooks/useAudio';
 
 interface GoingLiveTransitionProps {
   onComplete: () => void;
@@ -15,6 +16,7 @@ export function GoingLiveTransition({ onComplete }: GoingLiveTransitionProps) {
   });
 
   useEffect(() => {
+    playAudioCue('going-live');
     const timer = setTimeout(onComplete, 2500);
     return () => clearTimeout(timer);
   }, [onComplete]);
diff --git a/src/renderer/views/PillView.tsx b/src/renderer/views/PillView.tsx
index 6858431..db4bff9 100644
--- a/src/renderer/views/PillView.tsx
+++ b/src/renderer/views/PillView.tsx
@@ -1,4 +1,3 @@
-import React from 'react'
 import { motion } from 'framer-motion'
 import { useShowStore, selectCurrentAct } from '../stores/showStore'
 import { useTimer } from '../hooks/useTimer'
@@ -11,57 +10,12 @@ export function PillView() {
   const phase = useShowStore((s) => s.phase)
   const toggleExpanded = useShowStore((s) => s.toggleExpanded)
   const currentAct = useShowStore(selectCurrentAct)
-  const beatsLocked = useShowStore((s) => s.beatsLocked)
-  const beatThreshold = useShowStore((s) => s.beatThreshold)
   const { minutes, seconds, isRunning } = useTimer()
 
   const isUrgent = minutes < 5 && isRunning
 
   const timerText = `${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`
 
-  let content: React.ReactNode
-
-  if (phase === 'live') {
-    content = (
-      <>
-        <TallyLight isLive={true} size="lg" />
-        <span className="font-body text-sm font-medium text-txt-primary truncate flex-1">
-          {currentAct?.name}
-        </span>
-        <span
-          className={cn(
-            'font-mono text-sm font-semibold tabular-nums',
-            isUrgent ? 'text-beat animate-warm-pulse' : 'text-txt-primary'
-          )}
-        >
-          {timerText}
-        </span>
-        <BeatCounter size="sm" />
-      </>
-    )
-  } else if (phase === 'intermission') {
-    content = (
-      <>
-        <TallyLight isLive={false} />
-        <span className="font-body text-sm text-txt-secondary flex-1">
-          Intermission
-        </span>
-        <span className="font-body text-xs text-txt-muted">
-          no rush
-        </span>
-      </>
-    )
-  } else if (phase === 'strike') {
-    content = (
-      <>
-        <span className="font-body text-sm text-txt-primary flex-1 animate-golden-glow">
-          Show complete!
-        </span>
-        <BeatCounter size="sm" />
-      </>
-    )
-  }
-
   const showStrip = phase === 'live' || phase === 'intermission'
 
   return (
@@ -71,16 +25,62 @@ export function PillView() {
         'bg-surface/85 backdrop-blur-[20px]',
         'border border-white/[0.06]',
         'shadow-[0_8px_32px_rgba(0,0,0,0.4)]',
-        'cursor-pointer select-none'
+        'select-none'
       )}
-      onClick={toggleExpanded}
       initial={{ scale: 0.8, opacity: 0 }}
       animate={{ scale: 1, opacity: 1 }}
       exit={{ scale: 0.8, opacity: 0 }}
       transition={{ type: 'spring', stiffness: 400, damping: 30 }}
     >
       <div className="flex items-center gap-3 py-2.5 px-4">
-        {content}
+        {/* Left 48px: drag handle */}
+        <div className="w-12 shrink-0 flex items-center justify-center drag-region cursor-grab">
+          {phase === 'live' ? (
+            <TallyLight isLive={true} size="lg" />
+          ) : phase === 'intermission' ? (
+            <TallyLight isLive={false} />
+          ) : null}
+        </div>
+        {/* Rest: click to expand */}
+        <div
+          className="flex items-center gap-3 flex-1 cursor-pointer no-drag"
+          onClick={toggleExpanded}
+        >
+          {phase === 'live' && (
+            <>
+              <span className="font-body text-sm font-medium text-txt-primary truncate flex-1">
+                {currentAct?.name}
+              </span>
+              <span
+                className={cn(
+                  'font-mono text-sm font-semibold tabular-nums',
+                  isUrgent ? 'text-beat animate-warm-pulse' : 'text-txt-primary'
+                )}
+              >
+                {timerText}
+              </span>
+              <BeatCounter size="sm" />
+            </>
+          )}
+          {phase === 'intermission' && (
+            <>
+              <span className="font-body text-sm text-txt-secondary flex-1">
+                Intermission
+              </span>
+              <span className="font-body text-xs text-txt-muted">
+                no rush
+              </span>
+            </>
+          )}
+          {phase === 'strike' && (
+            <>
+              <span className="font-body text-sm text-txt-primary flex-1 animate-golden-glow">
+                Show complete!
+              </span>
+              <BeatCounter size="sm" />
+            </>
+          )}
+        </div>
       </div>
       {showStrip && <MiniRundownStrip />}
     </motion.div>
diff --git a/src/renderer/views/StrikeView.tsx b/src/renderer/views/StrikeView.tsx
index 5226a90..d7de3ef 100644
--- a/src/renderer/views/StrikeView.tsx
+++ b/src/renderer/views/StrikeView.tsx
@@ -1,10 +1,21 @@
-import { useShowStore, selectCompletedActs, selectSkippedActs } from '../stores/showStore'
+import { useEffect, useMemo } from 'react'
+import { useShowStore } from '../stores/showStore'
 import { ShowVerdict } from '../components/ShowVerdict'
 import { OnAirIndicator } from '../components/OnAirIndicator'
 import { BeatCounter } from '../components/BeatCounter'
 import { Button } from '../ui/button'
 import { motion } from 'framer-motion'
 import { cn } from '../lib/utils'
+import { playAudioCue } from '../hooks/useAudio'
+
+function formatDuration(startMs: number): string {
+  const elapsed = Date.now() - startMs
+  const totalMinutes = Math.round(elapsed / 60000)
+  const hours = Math.floor(totalMinutes / 60)
+  const mins = totalMinutes % 60
+  if (hours === 0) return `${mins}m`
+  return `${hours}h ${mins}m`
+}
 
 export function StrikeView() {
   const verdict = useShowStore((s) => s.verdict)
@@ -13,12 +24,26 @@ export function StrikeView() {
   const beatThreshold = useShowStore((s) => s.beatThreshold)
   const resetShow = useShowStore((s) => s.resetShow)
   const setExpanded = useShowStore((s) => s.setExpanded)
-  const completedActs = useShowStore(selectCompletedActs)
-  const skippedActs = useShowStore(selectSkippedActs)
+  const enterWritersRoom = useShowStore((s) => s.enterWritersRoom)
+  const showStartedAt = useShowStore((s) => s.showStartedAt)
+
+  // Compute derived data inline to avoid selector referential instability
+  const completedActs = useMemo(() => acts.filter((a) => a.status === 'completed'), [acts])
+  const skippedActs = useMemo(() => acts.filter((a) => a.status === 'skipped'), [acts])
+
+  const showDuration = useMemo(
+    () => (showStartedAt ? formatDuration(showStartedAt) : null),
+    [showStartedAt],
+  )
+
+  // Play show-complete audio cue once on mount
+  useEffect(() => {
+    playAudioCue('show-complete')
+  }, [])
 
   return (
     <motion.div
-      className="w-[560px] max-h-[680px] bg-surface rounded-xl overflow-hidden flex flex-col"
+      className="w-[560px] max-h-[680px] bg-surface rounded-xl overflow-hidden flex flex-col relative"
       initial={{ opacity: 0 }}
       animate={{ opacity: 1 }}
       transition={{ type: 'spring', stiffness: 200, damping: 25 }}
@@ -80,6 +105,21 @@ export function StrikeView() {
             </div>
             <div className="text-xs text-txt-muted mt-1">Beats Locked</div>
           </motion.div>
+
+          {showDuration && (
+            <motion.div
+              className="text-center"
+              initial={{ opacity: 0, y: 12 }}
+              animate={{ opacity: 1, y: 0 }}
+              transition={{ type: 'spring', stiffness: 200, damping: 25, delay: 0.5 }}
+              data-testid="show-duration"
+            >
+              <div className="font-mono text-3xl font-bold text-txt-primary">
+                {showDuration}
+              </div>
+              <div className="text-xs text-txt-muted mt-1">Show Duration</div>
+            </motion.div>
+          )}
         </div>
 
         {/* Act recap panel */}
@@ -123,14 +163,58 @@ export function StrikeView() {
         </div>
       </div>
 
-      {/* Footer */}
-      <div className="px-8 py-6 flex items-center gap-3 border-t border-[#242428]">
-        <Button variant="accent" onClick={resetShow}>
-          New Show
-        </Button>
-        <Button variant="neutral" onClick={() => setExpanded(false)}>
-          Close
-        </Button>
+      {/* DAY WON confetti */}
+      {verdict === 'DAY_WON' && (
+        <div className="absolute inset-0 pointer-events-none overflow-hidden z-20">
+          {Array.from({ length: 24 }).map((_, i) => (
+            <div
+              key={i}
+              className="absolute w-2 h-2 rounded-full animate-confetti"
+              style={{
+                left: `${Math.random() * 100}%`,
+                backgroundColor: ['#f59e0b', '#ef4444', '#8b5cf6', '#22c55e', '#60a5fa', '#ec4899'][i % 6],
+                animationDelay: `${Math.random() * 1.5}s`,
+              }}
+            />
+          ))}
+        </div>
+      )}
+
+      {/* Footer — Celebration Actions */}
+      <div className="px-8 py-6 flex flex-col gap-3 border-t border-[#242428]">
+        {verdict === 'DAY_WON' && (
+          <motion.p
+            className="text-sm text-beat text-center font-medium mb-1"
+            initial={{ opacity: 0 }}
+            animate={{ opacity: 1 }}
+            transition={{ type: 'spring', stiffness: 200, damping: 25, delay: 0.8 }}
+          >
+            Standing ovation.
+          </motion.p>
+        )}
+        <div className="flex items-center gap-3">
+          <Button
+            variant="accent"
+            onClick={enterWritersRoom}
+            data-testid="encore-btn"
+          >
+            Add an Encore
+          </Button>
+          <Button
+            variant="neutral"
+            onClick={resetShow}
+            data-testid="plan-tomorrow-btn"
+          >
+            Plan Tomorrow
+          </Button>
+          <Button
+            variant="neutral"
+            onClick={() => setExpanded(false)}
+            data-testid="thats-a-wrap-btn"
+          >
+            That&apos;s a Wrap
+          </Button>
+        </div>
       </div>
     </motion.div>
   )
diff --git a/src/renderer/views/WritersRoomView.tsx b/src/renderer/views/WritersRoomView.tsx
index 642f331..8dd8d94 100644
--- a/src/renderer/views/WritersRoomView.tsx
+++ b/src/renderer/views/WritersRoomView.tsx
@@ -9,6 +9,12 @@ import { motion, AnimatePresence } from 'framer-motion'
 
 const springTransition = { type: 'spring' as const, stiffness: 300, damping: 30 }
 
+function formatDateLabel(): string {
+  return new Date()
+    .toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' })
+    .toUpperCase()
+}
+
 export function WritersRoomView() {
   const energy = useShowStore((s) => s.energy)
   const writersRoomStep = useShowStore((s) => s.writersRoomStep)
@@ -129,9 +135,14 @@ ${planText}`
       <div
         className="bg-[#151517] px-5 py-3 flex items-center justify-between border-b border-[#242428] drag-region"
       >
-        <span className="font-mono text-xs tracking-widest uppercase text-txt-muted">
-          SHOWTIME
-        </span>
+        <div className="flex items-center gap-3">
+          <span className="font-mono text-xs tracking-widest uppercase text-txt-muted">
+            SHOWTIME
+          </span>
+          <span className="font-mono text-[10px] tracking-wider text-txt-muted/60" data-testid="date-label">
+            {formatDateLabel()}
+          </span>
+        </div>
         <button
           onClick={() => window.clui.quit()}
           className="text-txt-muted hover:text-onair transition-colors text-sm no-drag"

Output format (STRICT - follow exactly):
VERDICT: PASS or FAIL
FINDINGS:
- [severity] description (file:line)
Severity levels: Critical, High, Medium, Low

If no issues found, output:
VERDICT: PASS
FINDINGS:
- None