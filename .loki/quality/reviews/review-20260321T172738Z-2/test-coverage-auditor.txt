VERDICT: PASS
FINDINGS:
- None

**Rationale:** This diff is a pure dead-code cleanup with no behavioral changes:

1. **Removed constants (`BAR_WIDTH`, `PILL_HEIGHT`)** — confirmed via grep these are no longer referenced anywhere in source code. `createWindow()` already uses `VIEW_DIMENSIONS.expanded` (line 103). No test gap.

2. **Comment updates on no-op IPC handlers** — `RESIZE_HEIGHT`, `SET_WINDOW_WIDTH`, `ANIMATE_HEIGHT` remain no-ops with identical behavior (empty function bodies). Comments changed from "fixed height" to "sizing handled by SET_VIEW_MODE". No behavioral change, no test gap.

3. **The `SET_VIEW_MODE` handler** (line 389) does the actual work and has no E2E coverage in `e2e/showtime.test.ts`, but this handler was **not modified in this diff** — it's pre-existing code. The diff only updated comments that reference it.

4. **Documentation files** (`.loki/CONTINUITY.md`, `docs/plans/pipeline-journal.md`) — no code, no tests needed.
